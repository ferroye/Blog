import { combineReducers } from 'redux';

const lineReducer = (state, action) => {
    switch (action.type) {
        default:
            return null;
    }
}
export default combineReducers({
    test: lineReducer
});